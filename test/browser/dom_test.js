/* global cohesive */
describe('cohesive.dom (browser)', function() {
  it('should expose `cohesive.dom` in the global path', function() {
    assert.isNotNull(cohesive.dom)
  })

  var dom = cohesive.dom

})
